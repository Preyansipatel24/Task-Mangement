@using TaskManagementV1.Models.ResponseModels

@{
    ViewBag.PageTitle = "Dashboard";
    List<GetTaskListResModel> TodayTaskList = new List<GetTaskListResModel>();
    List<GetTaskListResModel> YesterdayTaskList = new List<GetTaskListResModel>();

    if (Model.Status)
    {
        List<GetTaskListResModel> resModels = Model.Data;
        TodayTaskList = resModels.Where(x => x.TaskDate.Date == DateTime.Today.Date).ToList();
        YesterdayTaskList = resModels.Where(x => x.TaskDate.Date == DateTime.Today.Date.AddDays(-1)).ToList();
    }
    TimeSpan TodayTotalHour = TodayTaskList.Count > 0 ? TodayTaskList.Select(x => TimeSpan.Parse(x.TaskDuration)).Aggregate((total, next) => total.Add(next)) : new TimeSpan();
    TimeSpan YesterdayTotalHour = YesterdayTaskList.Count > 0 ? YesterdayTaskList.Select(x => TimeSpan.Parse(x.TaskDuration)).Aggregate((total, next) => total.Add(next)) : new TimeSpan();

}
<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0"> Dashboard</h1>
            </div><!-- /.col -->
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Dashboard</a></li>
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->
<!-- Main content -->
<div class="content">
    <div class="container">

        @*  <div class="row">
        <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-info">
        <div class="inner">
        <h3>150</h3>

        <p>Total Projects</p>
        </div>
        <div class="icon">
        <i class="ion ion-bag"></i>
        </div>
        <a href="/Project/Index" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
        </div>
        </div>
        <!-- ./col -->
        <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-success">
        <div class="inner">
        <h3>53<sup style="font-size: 20px">%</sup></h3>

        <p>Bounce Rate</p>
        </div>
        <div class="icon">
        <i class="ion ion-stats-bars"></i>
        </div>
        <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
        </div>
        </div>
        <!-- ./col -->
        <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-warning">
        <div class="inner">
        <h3>44</h3>

        <p>User Registrations</p>
        </div>
        <div class="icon">
        <i class="ion ion-person-add"></i>
        </div>
        <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
        </div>
        </div>
        <!-- ./col -->
        <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-danger">
        <div class="inner">
        <h3>65</h3>

        <p>Unique Visitors</p>
        </div>
        <div class="icon">
        <i class="ion ion-pie-graph"></i>
        </div>
        <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a>
        </div>
        </div>
        <!-- ./col -->
        </div> *@

        <div class="row">
            <div class="col-md-12">
                <div class="card card-info">
                    <div class="card-header">
                        <h3 class="card-title">
                            Today's Task
                        </h3>
                        <h3 class="card-title float-right">
                            @if (TodayTotalHour.TotalHours >= 8)
                            {
                                <span class="badge badge-pill badge-success"> Total @TodayTotalHour.ToString(@"hh\:mm") Hours </span>
                            }
                            else if (TodayTotalHour.TotalHours < 5)
                            {
                                <span class="badge badge-pill badge-danger"> Total @TodayTotalHour.ToString(@"hh\:mm") Hours </span>
                            }
                            else
                            {
                                <span class="badge badge-pill badge-warning"> Total @TodayTotalHour.ToString(@"hh\:mm") Hours </span>
                            }
                        </h3>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        @foreach (var item in TodayTaskList)
                        {
                            <div class="callout callout-success">
                                <h5 class="float-right">@item.TaskDuration hours in <span class="badge badge badge-secondary"> @item.ProjectName </span></h5>
                                <p>@item.TaskDescription</p>
                            </div>
                        }
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <div class="card card-info">
                    <div class="card-header">
                        <h3 class="card-title">
                            Yesterday's Task
                        </h3>
                        <h3 class="card-title float-right">
                            @if (YesterdayTotalHour.TotalHours >= 8)
                            {
                                <span class="badge badge-pill badge-success"> Total @YesterdayTotalHour.ToString(@"hh\:mm") Hours</span>
                            }
                            else if (YesterdayTotalHour.TotalHours < 5)
                            {
                                <span class="badge badge-pill badge-danger"> Total @YesterdayTotalHour.ToString(@"hh\:mm") Hours</span>
                            }
                            else
                            {
                                <span class="badge badge-pill badge-warning"> Total @YesterdayTotalHour.ToString(@"hh\:mm") Hours</span>
                            }
                        </h3>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        @foreach (var item in YesterdayTaskList)
                        {
                            <div class="callout callout-success">
                                <h5 class="float-right">@item.TaskDuration hours in <span class="badge badge badge-secondary"> @item.ProjectName </span></h5>
                                <p>@item.TaskDescription</p>
                            </div>
                        }
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
        </div>

        @* <div class="row">
        <div class="col-md-12">
        <div class="card card-primary card-outline">
        <div class="card-header">
        <h5 class="card-title m-0">Dashboard</h5>
        </div>
        <div class="card-body">
        <h6 class="card-title">Special title treatment</h6>

        <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
        <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
        </div>
        </div>
        </div>
        *@
    </div><!-- /.container-fluid -->
</div>
<!-- /.content -->
